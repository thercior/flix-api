[uwsgi]

chdir           = /flix-api
module          = config.wsgi
home            = /flix-api/venv
master          = true
processes       = 10
socket          = /flix-api/flix-api.sock
chmod-socket    = 666
vacuum          = true